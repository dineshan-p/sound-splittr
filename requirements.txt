# Python dependencies for Sound Splittr
# Install with: pip install -r requirements.txt
#
# GPU support: PyTorch with CUDA is installed automatically via pip.
# Use the CPU-only wheel if no NVIDIA GPU:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# Core: Demucs AI stem separation engine
demucs>=4.0

# Audio I/O
soundfile>=0.13.1

# Signal processing (resampling, spectral analysis)
scipy>=1.11.0

# MP3 export
# 0.25.1 on PyPI lacks pyaudioop.py shim needed for Python 3.13+;
# install from git master which includes the fix (PR #816).
pydub @ git+https://github.com/jiaaro/pydub.git

# ML / AI runtime
torch>=2.5.1
torchaudio>=2.5.1
numpy>=1.26.4

# CLI framework
click>=8.1.7

# Backend REST API
fastapi>=0.115.0
uvicorn>=0.30.0

# Testing
pytest>=8.3.0
pytest-cov>=4.1.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
