# Python dependencies for stem splitter
# Install with: pip install -r requirements.txt
#
# GPU support note:
#   After installing this file, run:
#     pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121  # Linux (NVIDIA)
#     or use CPU-only if no GPU available.
#   See requirements-gpu.txt for GPU-specific versions.

demucs>=4.0
soundfile>=0.13.1
numpy>=1.26.4
pandas>=2.2.0
pydub>=0.29.0
pyyaml>=6.0
python-dotenv>=1.0.0
# GPU support (optional but recommended for live DJ)
torch>=2.5.1
torchaudio>=2.5.1
# Audio resampling & spectral analysis
scipy>=1.12.0
# CLI framework
click>=8.1.7
# Testing / quality assurance
pytest>=8.3.0
pytest-cov>=4.1.0
