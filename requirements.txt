# Python dependencies for Sound Splittr
# Install with: pip install -r requirements.txt
#
# GPU support: PyTorch with CUDA is installed automatically via pip.
# Use the CPU-only wheel if no NVIDIA GPU:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# Core: Demucs AI stem separation engine
demucs>=4.0

# Audio I/O
soundfile>=0.13.1

# ML / AI runtime
torch>=2.5.1
torchaudio>=2.5.1
numpy>=1.26.4

# CLI framework
click>=8.1.7

# Backend REST API
fastapi>=0.100.0
uvicorn>=0.23.0

# Testing
pytest>=8.3.0
pytest-cov>=4.1.0
